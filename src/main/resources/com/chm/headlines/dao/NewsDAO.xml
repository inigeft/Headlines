<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chm.headlines.dao.NewsDAO">
    <sql id="table">news</sql>
    <sql id="selectFields">id,title, link, image, like_count, comment_count,created_date,user_id, (like_count * POW(comment_count + 1, 2) + 1) / ((UNIX_TIMESTAMP(created_date) - UNIX_TIMESTAMP('2019-12-1 0:0')) / 3600 + 1) as score
    </sql>
    <select id="selectByUserIdAndOffset" resultType="com.chm.headlines.model.News">
        SELECT
        <include refid="selectFields"/>
        FROM
        <include refid="table"/>

        <if test="userId != 0">
            WHERE user_id = #{userId}
        </if>
        ORDER BY score DESC
        LIMIT #{offset},#{limit}
    </select>
</mapper>
